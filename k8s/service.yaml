apiVersion: v1
kind: Service
metadata:
  name: openai-gateway
  labels:
    app: openai-gateway
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: openai-gateway
